#1518. Water Bottles
def water_bottles(n, ex):
    full = empty = n 
    while empty >= ex:
        rem = empty // ex
        empty = empty // ex
        n+=rem
    return n
    
print(water_bottles(9,3))
