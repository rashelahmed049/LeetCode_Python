d = {}
class Solution:
    
    def climbStairs(self, n: int) -> int:
        if n in d:
            return d[n]
        if n ==1 or n <= 0:
            return 1
        d[n] = self.climbStairs(n-1) + self.climbStairs(n-2)
